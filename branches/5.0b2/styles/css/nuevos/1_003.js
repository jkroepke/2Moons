function resourceTicker(a){var c=this;c.config=a;c.htmlObj=document.getElementById(c.config.valueElem);var b=new Date();c.startTime=b.getTime();c.updateResource=function(){var d=new Date().getTime();nrResource=c.config.available+c.config.production*(d-c.startTime)/1000;nrResource=Math.max(0,Math.round(nrResource));if(nrResource<c.config.available||nrResource>=c.config.available&&nrResource<c.config.limit[1]){nrResource=gfNumberGetHumanReadable(nrResource);c.htmlObj.innerHTML=nrResource;if(nrResource>=c.config.limit[1]){c.htmlObj.className="overmark"}else{if(nrResource>=c.config.limit[1]*0.9&&nrResource<c.config.limit[1]){c.htmlObj.className="middlemark"}}}};if(a.intervalObj){clearInterval(a.intervalObj)}a.intervalObj=setInterval(c.updateResource,1000)};